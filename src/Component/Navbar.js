import React from "react";

export default function Navbar() {
  return (
    <div style={{          backgroundColor:"black",
    position: "sticky",
    top: 0 }}>
      <nav
        class="navbar navbar-dark "
        style={{

        }}
      >
        <div class="container-fluid">
          <a class="navbar-brand mx-auto fs-1">Dashboard</a>
        </div>
      </nav>
    </div>
  );
}
